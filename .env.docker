# Database Configuration
DB_HOST=postgres
DB_PORT=5432
DB_NAME=home_storage
DB_USER=storage_user
DB_PASSWORD=change_me_in_production

# Application Configuration
APP_PORT=3000
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Security: Change these values in production!
# DB_PASSWORD should be a strong password
# NEXT_PUBLIC_APP_URL should be your actual domain or server IP
